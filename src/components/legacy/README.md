# Legacy Components (moved from `src/`)

These components were previously sitting at the project root. They are kept here for reference/cleanup but are not wired into the current module/component architecture. Prefer the versions under `src/components` or the domain module components when building new features.
