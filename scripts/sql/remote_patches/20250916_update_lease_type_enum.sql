-- Deprecated remote patch: lease_type enum updates.
--
-- Phase 1 single-source-of-truth goal:
--   Only Supabase migrations under supabase/migrations/ should modify
--   enum definitions (including lease_type).
--
-- This file is retained for historical context only and MUST NOT be used
-- to apply schema changes. Add enum variants via new migrations instead.

