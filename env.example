# =============================================================================
# ENVIRONMENT VARIABLES TEMPLATE
# =============================================================================
## IMPORTANT
## - Do NOT put real secrets in this file.
## - Copy to `.env.local` for local dev and fill in real values.
## - Use hosting provider secrets and GitHub Actions secrets for non-local envs.
# =============================================================================

# Supabase Configuration (Required)
NEXT_PUBLIC_SUPABASE_URL="https://your-project-ref.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key-here"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key-here"

# Buildium Integration (Required)
# dev (sandbox): https://apisandbox.buildium.com/v1
# prod (live):   https://api.buildium.com/v1
BUILDIUM_BASE_URL="https://apisandbox.buildium.com/v1"
BUILDIUM_CLIENT_ID="your-client-id"
BUILDIUM_CLIENT_SECRET="your-client-secret"
BUILDIUM_WEBHOOK_SECRET="replace-with-32+char-random"

# App Configuration (Required)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Feature Flags
NEXT_PUBLIC_ENABLE_PROPERTY_ONBOARDING="false"

# Optional: Sentry Monitoring
SENTRY_DSN=""
SENTRY_TRACES_SAMPLE_RATE="0.1"
NEXT_PUBLIC_SENTRY_DSN=""
NEXT_PUBLIC_SENTRY_TRACES_SAMPLE_RATE="0.1"

# Optional: Centralized Logging
# Override log level (fatal,error,warn,info,debug,trace)
LOG_LEVEL="info"

# Optional: OpenTelemetry (Tracing)
# Base collector endpoint (no trailing slash), e.g. https://collector.example.com
OTEL_EXPORTER_OTLP_ENDPOINT=""
# Optional auth headers for the collector, e.g. "authorization=Bearer YOUR_TOKEN"
OTEL_EXPORTER_OTLP_HEADERS=""
# Optional service name override
OTEL_SERVICE_NAME="property-manager"

# Email Service - Resend (Required for Monthly Statements)
RESEND_API_KEY="re_your_api_key_here"
EMAIL_FROM_ADDRESS="noreply@yourdomain.com"
EMAIL_FROM_NAME="Property Management Company"

# Google OAuth (Gmail + Calendar)
GOOGLE_OAUTH_CLIENT_ID="your-google-oauth-client-id.apps.googleusercontent.com"
GOOGLE_OAUTH_CLIENT_SECRET="your-google-oauth-client-secret"
# Use a single callback for both flows; add GOOGLE_CALENDAR_OAUTH_REDIRECT_URI only if migrating
# from an existing calendar-specific redirect to avoid reissuing refresh tokens immediately.
GOOGLE_OAUTH_REDIRECT_URI="http://localhost:3000/api/auth/google/callback"
GOOGLE_CALENDAR_OAUTH_REDIRECT_URI=""
# Optional: Custom encryption key for Gmail tokens (defaults to NEXTAUTH_SECRET if not set)
GMAIL_TOKEN_ENCRYPTION_KEY=""

# Company Information (For PDF Statements)
COMPANY_NAME="Your Property Management Company"
COMPANY_ADDRESS="123 Main St, Suite 100, Your City, ST 12345"
COMPANY_PHONE="(555) 123-4567"
COMPANY_EMAIL="info@yourcompany.com"
COMPANY_LOGO_URL="https://yourcompany.com/logo.png"

# Google Maps API key (Required for map components; restrict by HTTP referrer/IP)
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY="your-google-maps-api-key-1234567890"

# NYC Address Enrichment (Geoservice + Open Data)
NYC_GEOSERVICE_API_KEY="your-nyc-planning-api-key"
NYC_GEOSERVICE_BASE_URL="https://geoservice.planning.nyc.gov/geoservice/geoservice.svc/"
NYC_OPEN_DATA_APP_TOKEN="your-nyc-open-data-token"
NYC_OPEN_DATA_BASE_URL="https://data.cityofnewyork.us/"


# =============================================================================
# DEPRECATED VARIABLES (REMOVED)
# =============================================================================
# The following variables are no longer needed:
# - EMAIL_SERVER_HOST, EMAIL_SERVER_PORT, EMAIL_SERVER_USER, EMAIL_SERVER_PASSWORD, EMAIL_FROM
# - DATABASE_URL (Prisma - replaced by Supabase)
# =============================================================================
