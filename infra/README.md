# Infra

This folder holds infrastructure and deployment configuration. Add any hosting/monitoring IaC, pipeline configs, or ops runbooks here to keep them out of the app codebase. If new deployment configs are introduced, prefer placing them under `infra/` rather than the repo root.
